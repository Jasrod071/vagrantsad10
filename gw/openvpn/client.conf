<LDAP>
    # IP de tu servidor IDP
    URL             ldap://172.2.10.2
    # Usuario con permisos de lectura (ajusta tu dc=...)
    BindDN          "cn=admin,dc=easyconfigpro,dc=org"
    Password        "adeasysl"
    Timeout         15
    TLSEnable       no
</LDAP>

<Authorization>
    # Dónde buscar a los usuarios
    BaseDN          "ou=ou_usuarios,dc=easyconfigpro,dc=org"
    # Filtro para encontrar al usuario por su login
    SearchFilter    "(uid=%u)"
    RequireGroup    false
</Authorization>
